general:
  build_dir: FindersKeepers

dependencies:
  pre:
    - rm ./gradlew
    - wget "https://services.gradle.org/distributions/gradle-2.14.1-bin.zip"; unzip gradle-2.14.1-bin.zip
  
test:
  pre:
    - pip install flask
  override:
    - python -m unittest discover -s ./server/tests -v
    - gradle test

