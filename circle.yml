general:
  build_dir: FindersKeepers

dependencies:
  pre:
    - rm ./gradlew
  
test:
  pre:
    - pip install flask
  override:
    - python -m unittest discover -s ./server/tests -v
    - gradle test

